article write give program run workstation base window system one pop window run top window mean terminal window one application may start terminal terminal may still exist may condition pop top may even may want consider window manager like relevant window virtual outside real terminal provide environment variable give window id even thing present environment may want may window different server example mouse